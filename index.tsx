import React, { useState, FC, FormEvent, useEffect, useMemo, createContext, useContext, useRef } from 'react';
import { createRoot } from 'react-dom/client';
import { GoogleGenAI, Type } from "@google/genai";

const API_KEY = process.env.API_KEY;
if (!API_KEY) {
    throw new Error("API_KEY environment variable not set.");
}
const ai = new GoogleGenAI({ apiKey: API_KEY });

// --- I18n & Translations ---
const translations = {
    en: {
        languageName: "English",
        app: { title: "FarmConnect+", subtitle: "Connecting Fields to the Future" },
        login: {
            farmerButton: "I'm a Farmer",
            buyerButton: "I'm a Buyer",
            googleSignIn: "Sign in with Google",
            orSignInWith: "Or sign in with",
        },
        farmer: {
            dashboardTitle: "Farmer Dashboard",
            profileTitle: "Your Profile",
            ordersTitle: "Incoming Orders",
            communityButton: "Community & Knowledge Hub",
            listProduceTitle: "List Your Produce",
            cropNameLabel: "Crop Name",
            unitLabel: "Unit (e.g., kg, piece, dozen)",
            priceLabel: "Price (per unit) in ₹",
            listCropButton: "List This Crop",
            myCropsTitle: "My crops",
            cropHealthTitle: "Crop Health & Tips",
            getTipButton: "Get Tip for {{cropName}}",
            gettingTip: "Getting Tip...",
            error: {
                aiPrice: "Could not fetch AI price suggestion. Please try again.",
                formValidation: "Please fill in all fields with valid values.",
                cropTip: "Could not fetch crop tip. Please try again."
            },
            profile: {
                kisanCard: "Digital Kisan Card",
                rewardsTitle: "Gamification & Rewards",
                rewardsDesc: "Complete tasks to earn new badges.",
                simulateDelivery: "Simulate Timely Delivery",
                logout: "Logout",
                backToDashboard: "← Back to Dashboard"
            },
            orders: {
                sortBy: "Sort by:",
                sort: { newest: "Newest First", oldest: "Oldest First", status: "Status" },
                orderFrom: "Order from {{name}}",
                total: "Total:",
                markShipped: "Mark as Shipped",
                markDelivered: "Mark as Delivered",
                delivered: "Delivered",
                noOrders: "You have no pending orders."
            }
        },
        buyer: {
            marketplaceTitle: "Marketplace",
            cartTitle: "Your Cart",
            profileTitle: "Your Profile",
            sellersTitle: "Sellers for {{produce}}",
            ordersTitle: "Your Orders",
            searchPlaceholder: "🔍 Search by crop name...",
            filters: { price: "Price", all: "All", under50: "Under ₹50", over50: "₹50 & Over", category: "Category", vegetables: "Vegetables", fruits: "Fruits" },
            freshFromFarms: "Fresh From The Farms",
            fromPrice: "From ₹{{price}}",
            viewSellers: "View Sellers",
            noResults: "No produce matches your filters. Try adjusting your search.",
            error: {
                bundle: "Could not generate a bundle. Please try again."
            },
            profile: {
                valuedCustomer: "Valued Customer",
                communityHub: "Community Hub",
                aiBundleTitle: "AI Recommended Bundle",
                aiBundleButton: "Generate a Bundle Idea",
                aiBundleGenerating: "Generating...",
                logout: "Logout",
                backToMarket: "← Back to Market"
            },
            cart: {
                from: "From: {{name}}",
                total: "Total:",
                checkout: "Proceed to Checkout",
                empty: "Your cart is empty. Add some fresh produce from the market!",
                backToMarket: "← Back to Market"
            },
            orders: {
                filter: { all: "All", pending: "Pending", shipped: "Shipped", delivered: "Delivered" },
                orderTo: "Order to {{name}}",
                noOrders: "You have no orders matching this status."
            }
        },
        community: {
            title: "Community Hub",
            back: "← Back",
            askTitle: "Ask the AI Crop Advisor",
            askPlaceholder: "Ask about pests, soil, irrigation, etc.",
            askButton: "Get Advice",
            askThinking: "Thinking...",
            error: "Could not get an answer from the AI Advisor. Please try again."
        },
        modal: {
            addToCartTitle: "Add {{produce}} to Cart",
            from: "From: {{name}}",
            price: "Price: ₹{{price}} / {{unit}}",
            quantity: "Quantity ({{unit}})",
            addButton: "Add to Cart"
        },
        badges: {
            'eco-farmer': 'Eco-Friendly Farmer',
            'trusted-buyer': 'Trusted Buyer',
            'timely-delivery': 'Timely Delivery',
            'first-purchase': 'First Purchase!'
        },
        placeholders: {
            price: "e.g., 45.50"
        },
        status: {
            pending: 'Pending',
            shipped: 'Shipped',
            delivered: 'Delivered'
        },
        alerts: {
            orderPlaced: "Order placed successfully with {{count}} seller(s)!",
            newBadge: "New Badge Earned: {{badgeName}}!",
            consistentDelivery: "You are consistently great at deliveries!",
            firstPurchase: 'Congratulations! You earned the "First Purchase" badge!'
        }
    },
    hi: {
        languageName: "हिन्दी",
        app: { title: "फार्मकनेक्ट+", subtitle: "खेतों को भविष्य से जोड़ना" },
        login: {
            farmerButton: "मैं एक किसान हूँ",
            buyerButton: "मैं एक खरीदार हूँ",
            googleSignIn: "Google से साइन इन करें",
            orSignInWith: "या इससे साइन इन करें",
        },
        farmer: {
            dashboardTitle: "किसान डैशबोर्ड",
            profileTitle: "आपकी प्रोफ़ाइल",
            ordersTitle: "आने वाले ऑर्डर",
            communityButton: "समुदाय और ज्ञान केंद्र",
            listProduceTitle: "अपनी उपज सूचीबद्ध करें",
            cropNameLabel: "फसल का नाम",
            unitLabel: "इकाई (जैसे, किलो, पीस, दर्जन)",
            priceLabel: "मूल्य (प्रति यूनिट) ₹ में",
            listCropButton: "इस फसल को सूचीबद्ध करें",
            myCropsTitle: "मेरी फसलें",
            cropHealthTitle: "फसल स्वास्थ्य और सुझाव",
            getTipButton: "{{cropName}} के लिए सुझाव प्राप्त करें",
            gettingTip: "सुझाव मिल रहा है...",
            error: {
                aiPrice: "AI मूल्य सुझाव प्राप्त नहीं हो सका। कृपया पुनः प्रयास करें।",
                formValidation: "कृपया सभी फ़ील्ड वैध मानों से भरें।",
                cropTip: "फसल सुझाव प्राप्त नहीं हो सका। कृपया पुनः प्रयास करें।"
            },
            profile: {
                kisanCard: "डिजिटल किसान कार्ड",
                rewardsTitle: "गेमिफिकेशन और पुरस्कार",
                rewardsDesc: "नए बैज अर्जित करने के लिए कार्य पूरे करें।",
                simulateDelivery: "समय पर डिलीवरी का अनुकरण करें",
                logout: "लॉग आउट",
                backToDashboard: "← डैशबोर्ड पर वापस"
            },
            orders: {
                sortBy: "क्रमबद्ध करें:",
                sort: { newest: "नवीनतम पहले", oldest: "सबसे पुराना पहले", status: "स्थिति" },
                orderFrom: "{{name}} से ऑर्डर",
                total: "कुल:",
                markShipped: "शिप्ड के रूप में चिह्नित करें",
                markDelivered: "डिलीवर के रूप में चिह्नित करें",
                delivered: "डिलीवर हो गया",
                noOrders: "आपके पास कोई लंबित ऑर्डर नहीं है।"
            }
        },
        buyer: {
            marketplaceTitle: "बाज़ार",
            cartTitle: "आपकी कार्ट",
            profileTitle: "आपकी प्रोफ़ाइल",
            sellersTitle: "{{produce}} के लिए विक्रेता",
            ordersTitle: "आपके ऑर्डर",
            searchPlaceholder: "🔍 फसल के नाम से खोजें...",
            filters: { price: "मूल्य", all: "सभी", under50: "₹50 से कम", over50: "₹50 और अधिक", category: "श्रेणी", vegetables: "सब्जियां", fruits: "फल" },
            freshFromFarms: "खेतों से ताज़ा",
            fromPrice: "₹{{price}} से",
            viewSellers: "विक्रेताओं को देखें",
            noResults: "आपकी खोज से कोई उपज मेल नहीं खाती। अपनी खोज समायोजित करने का प्रयास करें।",
            error: {
                bundle: "बंडल उत्पन्न नहीं किया जा सका। कृपया पुनः प्रयास करें।"
            },
            profile: {
                valuedCustomer: "मूल्यवान ग्राहक",
                communityHub: "सामुदायिक केंद्र",
                aiBundleTitle: "AI अनुशंसित बंडल",
                aiBundleButton: "एक बंडल आइडिया उत्पन्न करें",
                aiBundleGenerating: "उत्पन्न हो रहा है...",
                logout: "लॉग आउट",
                backToMarket: "← बाज़ार में वापस"
            },
            cart: {
                from: "से: {{name}}",
                total: "कुल:",
                checkout: "चेकआउट के लिए आगे बढ़ें",
                empty: "आपकी कार्ट खाली है। बाज़ार से कुछ ताज़ा उपज जोड़ें!",
                backToMarket: "← बाज़ार में वापस"
            },
            orders: {
                filter: { all: "सभी", pending: "लंबित", shipped: "शिप्ड", delivered: "डिलीवर" },
                orderTo: "{{name}} को ऑर्डर",
                noOrders: "इस स्थिति से मेल खाने वाले आपके कोई ऑर्डर नहीं हैं।"
            }
        },
        community: {
            title: "सामुदायिक केंद्र",
            back: "← वापस",
            askTitle: "AI फसल सलाहकार से पूछें",
            askPlaceholder: "कीटों, मिट्टी, सिंचाई आदि के बारे में पूछें।",
            askButton: "सलाह लें",
            askThinking: "सोच रहा है...",
            error: "AI सलाहकार से उत्तर प्राप्त नहीं हो सका। कृपया पुनः प्रयास करें।"
        },
        modal: {
            addToCartTitle: "{{produce}} को कार्ट में जोड़ें",
            from: "से: {{name}}",
            price: "मूल्य: ₹{{price}} / {{unit}}",
            quantity: "मात्रा ({{unit}})",
            addButton: "कार्ट में जोड़ें"
        },
        badges: {
            'eco-farmer': 'पर्यावरण-अनुकूल किसान',
            'trusted-buyer': 'विश्वसनीय खरीदार',
            'timely-delivery': 'समय पर डिलीवरी',
            'first-purchase': 'पहली खरीद!'
        },
        placeholders: {
            price: "उदा., 45.50"
        },
        status: {
            pending: 'लंबित',
            shipped: 'शिप्ड',
            delivered: 'डिलीवर'
        },
        alerts: {
            orderPlaced: "{{count}} विक्रेता(ओं) के साथ सफलतापूर्वक ऑर्डर दिया गया!",
            newBadge: "नया बैज अर्जित: {{badgeName}}!",
            consistentDelivery: "आप लगातार डिलीवरी में बहुत अच्छे हैं!",
            firstPurchase: 'बधाई हो! आपने "पहली खरीद" बैज अर्जित किया है!'
        }
    },
    bn: {
        languageName: "বাংলা",
        app: { title: "ফার্মকানেক্ট+", subtitle: "মাঠকে ভবিষ্যতের সাথে সংযুক্ত করা" },
        login: {
            farmerButton: "আমি একজন কৃষক",
            buyerButton: "আমি একজন ক্রেতা",
            googleSignIn: "Google দিয়ে সাইন ইন করুন",
            orSignInWith: "অথবা সাইন ইন করুন",
        },
        farmer: {
            dashboardTitle: "কৃষক ড্যাশবোর্ড",
            profileTitle: "আপনার প্রোফাইল",
            ordersTitle: "আগত অর্ডার",
            communityButton: "সম্প্রদায় ও জ্ঞান কেন্দ্র",
            listProduceTitle: "আপনার পণ্য তালিকাভুক্ত করুন",
            cropNameLabel: "ফসলের নাম",
            unitLabel: "একক (যেমন, কেজি, পিস, ডজন)",
            priceLabel: "মূল্য (প্রতি একক) ₹-তে",
            listCropButton: "এই ফসল তালিকাভুক্ত করুন",
            myCropsTitle: "আমার ফসল",
            cropHealthTitle: "ফসল স্বাস্থ্য ও টিপস",
            getTipButton: "{{cropName}}-এর জন্য টিপ পান",
            gettingTip: "টিপ আনা হচ্ছে...",
            error: {
                aiPrice: "AI মূল্যের পরামর্শ আনা যায়নি। অনুগ্রহ করে আবার চেষ্টা করুন।",
                formValidation: "অনুগ্রহ করে সমস্ত ক্ষেত্র বৈধ মান দিয়ে পূরণ করুন।",
                cropTip: "ফসলের টিপ আনা যায়নি। অনুগ্রহ করে আবার চেষ্টা করুন।"
            },
            profile: {
                kisanCard: "ডিজিটাল কিষাণ কার্ড",
                rewardsTitle: "গেমিফিকেশন ও পুরস্কার",
                rewardsDesc: "নতুন ব্যাজ অর্জনের জন্য কাজ সম্পূর্ণ করুন।",
                simulateDelivery: "সময়মত ডেলিভারির অনুকরণ করুন",
                logout: "লগ আউট",
                backToDashboard: "← ড্যাশবোর্ডে ফিরে যান"
            },
            orders: {
                sortBy: "সাজান:",
                sort: { newest: "নতুন প্রথমে", oldest: "পুরানো প্রথমে", status: "অবস্থা" },
                orderFrom: "{{name}} থেকে অর্ডার",
                total: "মোট:",
                markShipped: "প্রেরিত হিসাবে চিহ্নিত করুন",
                markDelivered: "বিতরিত হিসাবে চিহ্নিত করুন",
                delivered: "বিতরিত",
                noOrders: "আপনার কোন মুলতুবি অর্ডার নেই।"
            }
        },
        buyer: {
            marketplaceTitle: "বাজার",
            cartTitle: "আপনার কার্ট",
            profileTitle: "আপনার প্রোফাইল",
            sellersTitle: "{{produce}}-এর জন্য বিক্রেতা",
            ordersTitle: "আপনার অর্ডার",
            searchPlaceholder: "🔍 ফসলের নাম দিয়ে অনুসন্ধান করুন...",
            filters: { price: "মূল্য", all: "সব", under50: "₹৫০ এর নিচে", over50: "₹৫০ এবং উপরে", category: "বিভাগ", vegetables: "শাকসবজি", fruits: "ফল" },
            freshFromFarms: "খামার থেকে তাজা",
            fromPrice: "₹{{price}} থেকে",
            viewSellers: "বিক্রেতাদের দেখুন",
            noResults: "আপনার ফিল্টারের সাথে কোনো পণ্য মেলে না। আপনার অনুসন্ধান সামঞ্জস্য করার চেষ্টা করুন।",
            error: {
                bundle: "একটি বান্ডেল তৈরি করা যায়নি। অনুগ্রহ করে আবার চেষ্টা করুন।"
            },
            profile: {
                valuedCustomer: "মূল্যবান গ্রাহক",
                communityHub: "সম্প্রদায় কেন্দ্র",
                aiBundleTitle: "AI প্রস্তাবিত বান্ডেল",
                aiBundleButton: "একটি বান্ডেল আইডিয়া তৈরি করুন",
                aiBundleGenerating: "তৈরি হচ্ছে...",
                logout: "লগ আউট",
                backToMarket: "← বাজারে ফিরে যান"
            },
            cart: {
                from: "থেকে: {{name}}",
                total: "মোট:",
                checkout: "চেকআউটের জন্য এগিয়ে যান",
                empty: "আপনার কার্ট খালি। বাজার থেকে কিছু তাজা পণ্য যোগ করুন!",
                backToMarket: "← বাজারে ফিরে যান"
            },
            orders: {
                filter: { all: "সব", pending: "মুলতুবি", shipped: "প্রেরিত", delivered: "বিতরিত" },
                orderTo: "{{name}}-কে অর্ডার করুন",
                noOrders: "এই স্থিতির সাথে মেলে এমন আপনার কোনো অর্ডার নেই।"
            }
        },
        community: {
            title: "সম্প্রদায় কেন্দ্র",
            back: "← পিছনে",
            askTitle: "AI ফসল উপদেষ্টাকে জিজ্ঞাসা করুন",
            askPlaceholder: "পোকামাকড়, মাটি, সেচ ইত্যাদি সম্পর্কে জিজ্ঞাসা করুন।",
            askButton: "পরামর্শ নিন",
            askThinking: "ভাবছে...",
            error: "AI উপদেষ্টার কাছ থেকে উত্তর পাওয়া যায়নি। অনুগ্রহ করে আবার চেষ্টা করুন।"
        },
        modal: {
            addToCartTitle: "{{produce}} কার্টে যোগ করুন",
            from: "থেকে: {{name}}",
            price: "মূল্য: ₹{{price}} / {{unit}}",
            quantity: "পরিমাণ ({{unit}})",
            addButton: "কার্টে যোগ করুন"
        },
        badges: {
            'eco-farmer': 'পরিবেশ-বান্ধব কৃষক',
            'trusted-buyer': 'বিশ্বস্ত ক্রেতা',
            'timely-delivery': 'সময়মত ডেলিভারি',
            'first-purchase': 'প্রথম ক্রয়!'
        },
        placeholders: {
            price: "যেমন, ৪৫.৫০"
        },
        status: {
            pending: 'মুলতুবি',
            shipped: 'প্রেরিত',
            delivered: 'বিতরিত'
        },
        alerts: {
            orderPlaced: "{{count}} জন বিক্রেতার সাথে সফলভাবে অর্ডার দেওয়া হয়েছে!",
            newBadge: "নতুন ব্যাজ অর্জিত: {{badgeName}}!",
            consistentDelivery: "আপনি ডেলিভারিতে ধারাবাহিকভাবে দুর্দান্ত!",
            firstPurchase: 'অভিনন্দন! আপনি "প্রথম ক্রয়" ব্যাজ অর্জন করেছেন!'
        }
    },
    te: {
        languageName: "తెలుగు",
        app: { title: "ఫామ్‌కనెక్ట్+", subtitle: "పొలాలను భవిష్యత్తుతో కలుపుతోంది" },
        login: {
            farmerButton: "నేను ఒక రైతును",
            buyerButton: "నేను ఒక కొనుగోలుదారుని",
            googleSignIn: "Googleతో సైన్ ఇన్ చేయండి",
            orSignInWith: "లేదా దీనితో సైన్ ఇన్ చేయండి",
        },
        farmer: {
            dashboardTitle: "రైతు డాష్‌బోర్డ్",
            profileTitle: "మీ ప్రొఫైల్",
            ordersTitle: "ఇన్‌కమింగ్ ఆర్డర్‌లు",
            communityButton: "సంఘం & జ్ఞాన కేంద్రం",
            listProduceTitle: "మీ పంటను జాబితా చేయండి",
            cropNameLabel: "పంట పేరు",
            unitLabel: "యూనిట్ (ఉదా., కిలో, పీస్, డజను)",
            priceLabel: "ధర (ఒక్కో యూనిట్‌కు) ₹లలో",
            listCropButton: "ఈ పంటను జాబితా చేయండి",
            myCropsTitle: "నా పంటలు",
            cropHealthTitle: "పంట ఆరోగ్యం & చిట్కాలు",
            getTipButton: "{{cropName}} కోసం చిట్కా పొందండి",
            gettingTip: "చిట్కా పొందుతోంది...",
            error: {
                aiPrice: "AI ధర సూచనను పొందలేకపోయాము. దయచేసి మళ్లీ ప్రయత్నించండి.",
                formValidation: "దయచేసి అన్ని ఫీల్డ్‌లను చెల్లుబాటు అయ్యే విలువలతో నింపండి.",
                cropTip: "పంట చిట్కాను పొందలేకపోయాము. దయచేసి మళ్లీ ప్రయత్నించండి."
            },
            profile: {
                kisanCard: "డిజిటల్ కిసాన్ కార్డ్",
                rewardsTitle: "గేమిఫికేషన్ & రివార్డులు",
                rewardsDesc: "కొత్త బ్యాడ్జ్‌లను సంపాదించడానికి పనులను పూర్తి చేయండి.",
                simulateDelivery: "సకాలంలో డెలివరీని అనుకరించండి",
                logout: "లాగ్ అవుట్",
                backToDashboard: "← డాష్‌బోర్డ్‌కు తిరిగి వెళ్ళు"
            },
            orders: {
                sortBy: "దీని ప్రకారం క్రమబద్ధీకరించు:",
                sort: { newest: "కొత్తవి ముందు", oldest: "పాతవి ముందు", status: "స్థితి" },
                orderFrom: "{{name}} నుండి ఆర్డర్",
                total: "మొత్తం:",
                markShipped: "రవాణా చేయబడినట్లుగా గుర్తించు",
                markDelivered: "అందజేయబడినట్లుగా గుర్తించు",
                delivered: "అందజేయబడింది",
                noOrders: "మీకు పెండింగ్‌లో ఆర్డర్‌లు లేవు."
            }
        },
        buyer: {
            marketplaceTitle: "మార్కెట్‌ప్లేస్",
            cartTitle: "మీ కార్ట్",
            profileTitle: "మీ ప్రొఫైల్",
            sellersTitle: "{{produce}} కోసం అమ్మకందారులు",
            ordersTitle: "మీ ఆర్డర్‌లు",
            searchPlaceholder: "🔍 పంట పేరుతో శోధించండి...",
            filters: { price: "ధర", all: "అన్నీ", under50: "₹50 లోపు", over50: "₹50 & అంతకంటే ఎక్కువ", category: "వర్గం", vegetables: "కూరగాయలు", fruits: "పండ్లు" },
            freshFromFarms: "పొలాల నుండి తాజాగా",
            fromPrice: "₹{{price}} నుండి",
            viewSellers: "అమ్మకందారులను వీక్షించండి",
            noResults: "మీ ఫిల్టర్‌లకు ఏ పంట సరిపోలలేదు. మీ శోధనను సర్దుబాటు చేయడానికి ప్రయత్నించండి.",
            error: {
                bundle: "బండిల్‌ను రూపొందించలేకపోయాము. దయచేసి మళ్లీ ప్రయత్నించండి."
            },
            profile: {
                valuedCustomer: "విలువైన కస్టమర్",
                communityHub: "సంఘ కేంద్రం",
                aiBundleTitle: "AI సిఫార్సు చేసిన బండిల్",
                aiBundleButton: "ఒక బండిల్ ఐడియాను రూపొందించండి",
                aiBundleGenerating: "రూపొందించబడుతోంది...",
                logout: "లాగ్ అవుట్",
                backToMarket: "← మార్కెట్‌కు తిరిగి వెళ్ళు"
            },
            cart: {
                from: "నుండి: {{name}}",
                total: "మొత్తం:",
                checkout: "చెక్‌అవుట్‌కు కొనసాగండి",
                empty: "మీ కార్ట్ ఖాళీగా ఉంది. మార్కెట్ నుండి కొన్ని తాజా పంటలను జోడించండి!",
                backToMarket: "← మార్కెట్‌కు తిరిగి వెళ్ళు"
            },
            orders: {
                filter: { all: "అన్నీ", pending: "పెండింగ్‌లో ఉంది", shipped: "రవాణా చేయబడింది", delivered: "అందజేయబడింది" },
                orderTo: "{{name}}కు ఆర్డర్ చేయండి",
                noOrders: "ఈ స్థితికి సరిపోయే ఆర్డర్‌లు మీకు లేవు."
            }
        },
        community: {
            title: "సంఘ కేంద్రం",
            back: "← వెనుకకు",
            askTitle: "AI పంట సలహాదారుని అడగండి",
            askPlaceholder: "తెగుళ్లు, నేల, నీటిపారుదల మొదలైన వాటి గురించి అడగండి.",
            askButton: "సలహా పొందండి",
            askThinking: "ఆలోచిస్తోంది...",
            error: "AI సలహాదారు నుండి సమాధానం పొందలేకపోయాము. దయచేసి మళ్లీ ప్రయత్నించండి."
        },
        modal: {
            addToCartTitle: "{{produce}}ను కార్ట్‌కు జోడించు",
            from: "నుండి: {{name}}",
            price: "ధర: ₹{{price}} / {{unit}}",
            quantity: "పరిమాణం ({{unit}})",
            addButton: "కార్ట్‌కు జోడించు"
        },
        badges: {
            'eco-farmer': 'పర్యావరణ అనుకూల రైతు',
            'trusted-buyer': 'విశ్వసనీయ కొనుగోలుదారు',
            'timely-delivery': 'సకాలంలో డెలివరీ',
            'first-purchase': 'మొదటి కొనుగోలు!'
        },
        placeholders: {
            price: "ఉదా., 45.50"
        },
        status: {
            pending: 'పెండింగ్‌లో ఉంది',
            shipped: 'రవాణా చేయబడింది',
            delivered: 'అందజేయబడింది'
        },
        alerts: {
            orderPlaced: "{{count}} విక్రేత(ల)తో ఆర్డర్ విజయవంతంగా చేయబడింది!",
            newBadge: "కొత్త బ్యాడ్జ్ సంపాదించారు: {{badgeName}}!",
            consistentDelivery: "మీరు డెలివరీలలో స్థిరంగా గొప్పవారు!",
            firstPurchase: 'అభినందనలు! మీరు "మొదటి కొనుగోలు" బ్యాడ్జ్‌ను సంపాదించారు!'
        }
    },
    kn: {
        languageName: "ಕನ್ನಡ",
        app: { title: "ಫಾರ್ಮ್‌ಕನೆಕ್ಟ್+", subtitle: "ಕ್ಷೇತ್ರಗಳನ್ನು ಭವಿಷ್ಯಕ್ಕೆ ಸಂಪರ್ಕಿಸುವುದು" },
        login: {
            farmerButton: "ನಾನು ರೈತ",
            buyerButton: "ನಾನು ಖರೀದಿದಾರ",
            googleSignIn: "Google ಮೂಲಕ ಸೈನ್ ಇನ್ ಮಾಡಿ",
            orSignInWith: "ಅಥವಾ ಇದರೊಂದಿಗೆ ಸೈನ್ ಇನ್ ಮಾಡಿ",
        },
        farmer: {
            dashboardTitle: "ರೈತರ ಡ್ಯಾಶ್‌ಬೋರ್ಡ್",
            profileTitle: "ನಿಮ್ಮ ಪ್ರೊಫೈಲ್",
            ordersTitle: "ಬರುತ್ತಿರುವ ಆರ್ಡರ್‌ಗಳು",
            communityButton: "ಸಮುದಾಯ ಮತ್ತು ಜ್ಞಾನ ಕೇಂದ್ರ",
            listProduceTitle: "ನಿಮ್ಮ ಉತ್ಪನ್ನವನ್ನು ಪಟ್ಟಿ ಮಾಡಿ",
            cropNameLabel: "ಬೆಳೆ ಹೆಸರು",
            unitLabel: "ಘಟಕ (ಉದಾ., ಕೆಜಿ, ಪೀಸ್, ಡಜನ್)",
            priceLabel: "ಬೆಲೆ (ಪ್ರತಿ ಘಟಕಕ್ಕೆ) ₹ ನಲ್ಲಿ",
            listCropButton: "ಈ ಬೆಳೆಯನ್ನು ಪಟ್ಟಿ ಮಾಡಿ",
            myCropsTitle: "ನನ್ನ ಬೆಳೆಗಳು",
            cropHealthTitle: "ಬೆಳೆ ಆರೋಗ್ಯ ಮತ್ತು ಸಲಹೆಗಳು",
            getTipButton: "{{cropName}} ಗಾಗಿ ಸಲಹೆ ಪಡೆಯಿರಿ",
            gettingTip: "ಸಲಹೆ ಪಡೆಯಲಾಗುತ್ತಿದೆ...",
            error: {
                aiPrice: "AI ಬೆಲೆ ಸಲಹೆಯನ್ನು ಪಡೆಯಲು ಸಾಧ್ಯವಾಗಲಿಲ್ಲ. ದಯವಿಟ್ಟು ಮತ್ತೆ ಪ್ರಯತ್ನಿಸಿ.",
                formValidation: "ದಯವಿಟ್ಟು ಎಲ್ಲಾ ಕ್ಷೇತ್ರಗಳನ್ನು ಮಾನ್ಯ ಮೌಲ್ಯಗಳೊಂದಿಗೆ ಭರ್ತಿ ಮಾಡಿ.",
                cropTip: "ಬೆಳೆ ಸಲಹೆಯನ್ನು ಪಡೆಯಲು ಸಾಧ್ಯವಾಗಲಿಲ್ಲ. ದಯವಿಟ್ಟು ಮತ್ತೆ ಪ್ರಯತ್ನಿಸಿ."
            },
            profile: {
                kisanCard: "ಡಿಜಿಟಲ್ ಕಿಸಾನ್ ಕಾರ್ಡ್",
                rewardsTitle: "ಗೇಮಿಫಿಕೇಶನ್ ಮತ್ತು ಬಹುಮಾನಗಳು",
                rewardsDesc: "ಹೊಸ ಬ್ಯಾಡ್ಜ್‌ಗಳನ್ನು ಗಳಿಸಲು ಕಾರ್ಯಗಳನ್ನು ಪೂರ್ಣಗೊಳಿಸಿ.",
                simulateDelivery: "ಸಮಯೋಚಿತ ವಿತರಣೆಯನ್ನು ಅನುಕರಿಸಿ",
                logout: "ಲಾಗ್ ಔಟ್",
                backToDashboard: "← ಡ್ಯಾಶ್‌ಬೋರ್ಡ್‌ಗೆ ಹಿಂತಿರುಗಿ"
            },
            orders: {
                sortBy: "ಇದರಂತೆ ವಿಂಗಡಿಸಿ:",
                sort: { newest: "ಹೊಸದು ಮೊದಲು", oldest: "ಹಳೆಯದು ಮೊದಲು", status: "ಸ್ಥಿತಿ" },
                orderFrom: "{{name}} ಅವರಿಂದ ಆರ್ಡರ್",
                total: "ಒಟ್ಟು:",
                markShipped: "ರವಾನಿಸಲಾಗಿದೆ ಎಂದು ಗುರುತಿಸಿ",
                markDelivered: "ವಿತರಿಸಲಾಗಿದೆ ಎಂದು ಗುರುತಿಸಿ",
                delivered: "ವಿತರಿಸಲಾಗಿದೆ",
                noOrders: "ನಿಮ್ಮ ಬಳಿ ಯಾವುದೇ ಬಾಕಿ ಇರುವ ಆರ್ಡರ್‌ಗಳಿಲ್ಲ."
            }
        },
        buyer: {
            marketplaceTitle: "ಮಾರುಕಟ್ಟೆ",
            cartTitle: "ನಿಮ್ಮ ಕಾರ್ಟ್",
            profileTitle: "ನಿಮ್ಮ ಪ್ರೊಫೈಲ್",
            sellersTitle: "{{produce}} ಗಾಗಿ ಮಾರಾಟಗಾರರು",
            ordersTitle: "ನಿಮ್ಮ ಆರ್ಡರ್‌ಗಳು",
            searchPlaceholder: "🔍 ಬೆಳೆ ಹೆಸರಿನಿಂದ ಹುಡುಕಿ...",
            filters: { price: "ಬೆಲೆ", all: "ಎಲ್ಲಾ", under50: "₹50 ಕ್ಕಿಂತ ಕಡಿಮೆ", over50: "₹50 ಮತ್ತು ಹೆಚ್ಚು", category: "ವರ್ಗ", vegetables: "ತರಕಾರಿಗಳು", fruits: "ಹಣ್ಣುಗಳು" },
            freshFromFarms: "ಹೊಲಗಳಿಂದ ತಾಜಾ",
            fromPrice: "₹{{price}} ರಿಂದ",
            viewSellers: "ಮಾರಾಟಗಾರರನ್ನು ವೀಕ್ಷಿಸಿ",
            noResults: "ನಿಮ್ಮ ಫಿಲ್ಟರ್‌ಗಳಿಗೆ ಯಾವುದೇ ಉತ್ಪನ್ನ ಹೊಂದಿಕೆಯಾಗುವುದಿಲ್ಲ. ನಿಮ್ಮ ಹುಡುಕಾಟವನ್ನು ಸರಿಹೊಂದಿಸಲು ಪ್ರಯತ್ನಿಸಿ.",
            error: {
                bundle: "ಬಂಡಲ್ ರಚಿಸಲು ಸಾಧ್ಯವಾಗಲಿಲ್ಲ. ದಯವಿಟ್ಟು ಮತ್ತೆ ಪ್ರಯತ್ನಿಸಿ."
            },
            profile: {
                valuedCustomer: "ಮೌಲ್ಯಯುತ ಗ್ರಾಹಕ",
                communityHub: "ಸಮುದಾಯ ಕೇಂದ್ರ",
                aiBundleTitle: "AI ಶಿಫಾರಸು ಮಾಡಿದ ಬಂಡಲ್",
                aiBundleButton: "ಒಂದು ಬಂಡಲ್ ಐಡಿಯಾ ರಚಿಸಿ",
                aiBundleGenerating: "ರಚಿಸಲಾಗುತ್ತಿದೆ...",
                logout: "ಲಾಗ್ ಔಟ್",
                backToMarket: "← ಮಾರುಕಟ್ಟೆಗೆ ಹಿಂತಿರುಗಿ"
            },
            cart: {
                from: "ಇವರಿಂದ: {{name}}",
                total: "ಒಟ್ಟು:",
                checkout: "ಚೆಕ್‌ಔಟ್‌ಗೆ ಮುಂದುವರಿಯಿರಿ",
                empty: "ನಿಮ್ಮ ಕಾರ್ಟ್ ಖಾಲಿಯಾಗಿದೆ. ಮಾರುಕಟ್ಟೆಯಿಂದ ಕೆಲವು ತಾಜಾ ಉತ್ಪನ್ನಗಳನ್ನು ಸೇರಿಸಿ!",
                backToMarket: "← ಮಾರುಕಟ್ಟೆಗೆ ಹಿಂತಿರುಗಿ"
            },
            orders: {
                filter: { all: "ಎಲ್ಲಾ", pending: "ಬಾಕಿ ಇದೆ", shipped: "ರವಾನಿಸಲಾಗಿದೆ", delivered: "ವಿತರಿಸಲಾಗಿದೆ" },
                orderTo: "{{name}} ಅವರಿಗೆ ಆರ್ಡರ್",
                noOrders: "ಈ ಸ್ಥಿತಿಗೆ ಸರಿಹೊಂದುವ ಯಾವುದೇ ಆರ್ಡರ್‌ಗಳಿಲ್ಲ."
            }
        },
        community: {
            title: "ಸಮುದಾಯ ಕೇಂದ್ರ",
            back: "← ಹಿಂದೆ",
            askTitle: "AI ಬೆಳೆ ಸಲಹೆಗಾರರನ್ನು ಕೇಳಿ",
            askPlaceholder: "ಕೀಟಗಳು, ಮಣ್ಣು, ನೀರಾವರಿ, ಇತ್ಯಾದಿಗಳ ಬಗ್ಗೆ ಕೇಳಿ.",
            askButton: "ಸಲಹೆ ಪಡೆಯಿರಿ",
            askThinking: "ಯೋಚಿಸುತ್ತಿದೆ...",
            error: "AI ಸಲಹೆಗಾರರಿಂದ ಉತ್ತರವನ್ನು ಪಡೆಯಲು ಸಾಧ್ಯವಾಗಲಿಲ್ಲ. ದಯವಿಟ್ಟು ಮತ್ತೆ ಪ್ರಯತ್ನಿಸಿ."
        },
        modal: {
            addToCartTitle: "{{produce}} ಅನ್ನು ಕಾರ್ಟ್‌ಗೆ ಸೇರಿಸಿ",
            from: "ಇವರಿಂದ: {{name}}",
            price: "ಬೆಲೆ: ₹{{price}} / {{unit}}",
            quantity: "ಪ್ರಮಾಣ ({{unit}})",
            addButton: "ಕಾರ್ಟ್‌ಗೆ ಸೇರಿಸಿ"
        },
        badges: {
            'eco-farmer': 'ಪರಿಸರ ಸ್ನೇಹಿ ರೈತ',
            'trusted-buyer': 'ವಿಶ್ವಾಸಾರ್ಹ ಖರೀದಿದಾರ',
            'timely-delivery': 'ಸಮಯೋಚಿತ ವಿತರಣೆ',
            'first-purchase': 'ಮೊದಲ ಖರೀದಿ!'
        },
        placeholders: {
            price: "ಉದಾ., 45.50"
        },
        status: {
            pending: 'ಬಾಕಿ ಇದೆ',
            shipped: 'ರವಾನಿಸಲಾಗಿದೆ',
            delivered: 'ವಿತರಿಸಲಾಗಿದೆ'
        },
        alerts: {
            orderPlaced: "{{count}} ಮಾರಾಟಗಾರ(ರ)ೊಂದಿಗೆ ಆರ್ಡರ್ ಯಶಸ್ವಿಯಾಗಿ ಮಾಡಲಾಗಿದೆ!",
            newBadge: "ಹೊಸ ಬ್ಯಾಡ್ಜ್ ಗಳಿಸಲಾಗಿದೆ: {{badgeName}}!",
            consistentDelivery: "ನೀವು ವಿತರಣೆಗಳಲ್ಲಿ ಸ್ಥಿರವಾಗಿ ಉತ್ತಮವಾಗಿದ್ದೀರಿ!",
            firstPurchase: 'ಅಭಿನಂದನೆಗಳು! ನೀವು "ಮೊದಲ ಖರೀದಿ" ಬ್ಯಾಡ್ಜ್ ಗಳಿಸಿದ್ದೀರಿ!'
        }
    }
};

type LanguageCode = keyof typeof translations;

interface LanguageContextType {
  language: LanguageCode;
  setLanguage: (lang: LanguageCode) => void;
  t: (key: string, options?: Record<string, string | number>) => string;
}

const LanguageContext = createContext<LanguageContextType | null>(null);
const useLanguage = () => {
    const context = useContext(LanguageContext);
    if (!context) {
        throw new Error("useLanguage must be used within a LanguageProvider");
    }
    return context;
};

// --- Data & Types ---
interface Farmer {
    id: string;
    name: string;
    location: string;
    rating: number; // e.g., 4.5
}

interface Listing {
    id: string;
    produceName: string;
    price: number;
    unit: string;
    img: string;
    category: string;
    seller: Farmer;
}

interface CartItem {
    listing: Listing;
    quantity: number;
}

interface Order {
    id: number; // Using timestamp for sortable ID
    buyerName: string;
    items: CartItem[];
    total: number;
    sellerId: string;
    status: 'Pending' | 'Shipped' | 'Delivered';
}

const mockFarmers: Farmer[] = [
    { id: 'farmer-1', name: 'Aisha Kumari', location: 'Nashik, Maharashtra', rating: 4.8 },
    { id: 'farmer-2', name: 'Rajesh Patel', location: 'Hoshiarpur, Punjab', rating: 4.5 },
    { id: 'farmer-3', name: 'Sunita Devi', location: 'Mettupalayam, Tamil Nadu', rating: 4.7 },
];

const initialProduceData = [
    // Fruits
    { name: 'Apple', unit: 'kg', img: 'https://source.unsplash.com/300x300/?apple', category: 'Fruit', prices: [120, 115, 125] },
    { name: 'Banana', unit: 'dozen', img: 'https://source.unsplash.com/300x300/?banana', category: 'Fruit', prices: [50, 55] },
    { name: 'Orange', unit: 'kg', img: 'https://source.unsplash.com/300x300/?orange', category: 'Fruit', prices: [80, 85] },
    { name: 'Mango', unit: 'kg', img: 'https://source.unsplash.com/300x300/?mango', category: 'Fruit', prices: [150, 160, 145] },
    // ... (rest of produce data is unchanged, omitted for brevity)
    { name: 'Potato', unit: 'kg', img: 'https://source.unsplash.com/300x300/?potato', category: 'Vegetable', prices: [25, 28, 24] },
    { name: 'Tomato', unit: 'kg', img: 'https://source.unsplash.com/300x300/?tomato', category: 'Vegetable', prices: [40, 38, 42] },
    { name: 'Onion', unit: 'kg', img: 'https://source.unsplash.com/300x300/?onion', category: 'Vegetable', prices: [30, 32] },
    { name: 'Carrot', unit: 'kg', img: 'https://source.unsplash.com/300x300/?carrot', category: 'Vegetable', prices: [50, 48] },
    { name: 'Cabbage', unit: 'piece', img: 'https://source.unsplash.com/300x300/?cabbage', category: 'Vegetable', prices: [30] },
    { name: 'Cauliflower', unit: 'piece', img: 'https://source.unsplash.com/300x300/?cauliflower', category: 'Vegetable', prices: [40] },
    { name: 'Broccoli', unit: 'piece', img: 'https://source.unsplash.com/300x300/?broccoli', category: 'Vegetable', prices: [80] },
    { name: 'Spinach', unit: 'bunch', img: 'https://source.unsplash.com/300x300/?spinach', category: 'Vegetable', prices: [20, 22] },
    { name: 'Mushroom', unit: 'kg', img: 'https://source.unsplash.com/300x300/?mushroom', category: 'Vegetable', prices: [250, 260] },
];

const initialListings: Listing[] = initialProduceData.flatMap((produce, index) =>
    produce.prices.map((price, priceIndex) => {
        const farmer = mockFarmers[(index + priceIndex) % mockFarmers.length];
        return {
            id: `${produce.name.toLowerCase().replace(' ', '-')}-${farmer.id}-${price}`,
            produceName: produce.name,
            price: price,
            unit: produce.unit,
            img: produce.img,
            category: produce.category,
            seller: farmer,
        };
    })
);

const uniqueProduceNames = [...new Set(initialProduceData.map(p => p.name))].sort();

interface Badge {
    id: string;
    nameKey: string;
    icon: JSX.Element;
    color: string;
}

// --- SVG Icons ---
const FingerprintIcon = () => (
    <svg xmlns="http://www.w3.org/2000/svg" height="24" viewBox="0 0 24 24" width="24"><path d="M0 0h24v24H0z" fill="none"/><path d="M15.5 12c-1.38 0-2.5 1.12-2.5 2.5s1.12 2.5 2.5 2.5 2.5-1.12 2.5-2.5-1.12-2.5-2.5-2.5zM9 12c-1.38 0-2.5 1.12-2.5 2.5s1.12 2.5 2.5 2.5 2.5-1.12 2.5-2.5-1.12-2.5-2.5-2.5zm6.5 5.5c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2zM9 17.5c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2zm-5-5C2.9 12.5 2 13.4 2 14.5s.9 2 2 2 2-.9 2-2-.9-2-2-2zm15-2c-1.1 0-2 .9-2 2s.9 2 2 2 2-.9 2-2-.9-2-2-2zm-7.5-5C9.62 5.5 8.5 6.62 8.5 8s1.12 2.5 2.5 2.5 2.5-1.12 2.5-2.5S12.38 5.5 11 5.5zm-4 3C5.9 8.5 5 9.4 5 10.5S5.9 12.5 7 12.5s2-.9 2-2-.9-2-2-2z"/></svg>
);
const GoogleIcon = () => (
    <svg version="1.1" xmlns="http://www.w3.org/2000/svg" width="18px" height="18px" viewBox="0 0 48 48"><g><path fill="#EA4335" d="M24 9.5c3.54 0 6.71 1.22 9.21 3.6l6.85-6.85C35.9 2.38 30.47 0 24 0 14.62 0 6.51 5.38 2.56 13.22l7.98 6.19C12.43 13.72 17.74 9.5 24 9.5z"></path><path fill="#4285F4" d="M46.98 24.55c0-1.57-.15-3.09-.42-4.55H24v9.02h12.94c-.58 2.96-2.26 5.48-4.78 7.18l7.73 6c4.51-4.18 7.09-10.36 7.09-17.65z"></path><path fill="#FBBC05" d="M10.53 28.59c-.48-1.45-.76-2.99-.76-4.59s.27-3.14.76-4.59l-7.98-6.19C.92 16.46 0 20.12 0 24c0 3.88.92 7.54 2.56 10.78l7.97-6.19z"></path><path fill="#34A853" d="M24 48c6.48 0 11.93-2.13 15.89-5.81l-7.73-6c-2.15 1.45-4.92 2.3-8.16 2.3-6.26 0-11.57-4.22-13.47-9.91l-7.98 6.19C6.51 42.62 14.62 48 24 48z"></path><path fill="none" d="M0 0h48v48H0z"></path></g></svg>
);
const LogoutIcon = () => (
    <svg xmlns="http://www.w3.org/2000/svg" height="24" viewBox="0 0 24 24" width="24"><path d="M0 0h24v24H0z" fill="none"/><path d="M17 7l-1.41 1.41L18.17 11H8v2h10.17l-2.58 2.58L17 17l5-5zM4 5h8V3H4c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h8v-2H4V5z"/></svg>
);
const LeafIcon = () => <svg xmlns="http://www.w3.org/2000/svg" height="24" viewBox="0 0 24 24" width="24"><path d="M0 0h24v24H0z" fill="none"/><path d="M17 11c.34 0 .67.04 1 .09V6.27L10.5 3 3 6.27v4.91c0 4.54 3.2 8.79 7.5 9.82.55-.13 1.08-.32 1.6-.55-.71-.8-1.23-1.77-1.48-2.82-.33-1.4.05-2.85.93-3.95l.95-1.18c.55-.68 1.42-1.13 2.39-1.13zM5 7.42L10.5 5l5.5 2.42v3.01c-.75-.24-1.54-.33-2.35-.15l-1.99.42c-1.48.31-2.73 1.5-3.16 2.98-.3.98-.24 2.01.18 2.92L5 12.18V7.42z"/><path d="M17 13c-2.21 0-4 1.79-4 4s1.79 4 4 4 4-1.79 4-4-1.79-4-4-4zm0 6l-1.41-1.41L17 16.17l1.41-1.41L20.83 17 17 20.83z"/></svg>;
const ShieldIcon = () => <svg xmlns="http://www.w3.org/2000/svg" height="24" viewBox="0 0 24 24" width="24"><path d="M0 0h24v24H0z" fill="none"/><path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4zm0 10.99h7c-.53 4.12-3.28 7.79-7 8.94V12H5V6.3l7-3.11v8.8z"/></svg>;
const TruckIcon = () => <svg xmlns="http://www.w3.org/2000/svg" height="24" viewBox="0 0 24 24" width="24"><path d="M0 0h24v24H0z" fill="none"/><path d="M20 8h-3V4H3c-1.1 0-2 .9-2 2v11h2c0 1.66 1.34 3 3 3s3-1.34 3-3h6c0 1.66 1.34 3 3 3s3-1.34 3-3h2v-5l-3-4zM6 18.5c-.83 0-1.5-.67-1.5-1.5s.67-1.5 1.5-1.5 1.5.67 1.5 1.5-.67 1.5-1.5 1.5zm13.5-9l1.96 2.5H17V9.5h2.5zM18 18.5c-.83 0-1.5-.67-1.5-1.5s.67-1.5 1.5-1.5 1.5.67 1.5 1.5-.67 1.5-1.5 1.5z"/></svg>;
const StarIcon = (props: { filled?: boolean }) => <svg xmlns="http://www.w3.org/2000/svg" height="16" viewBox="0 0 24 24" width="16" fill={props.filled ? "#FFC107" : "#E0E0E0"}><path d="M0 0h24v24H0z" fill="none"/><path d="M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z"/></svg>;
const CommunityIcon = () => <svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 0 24 24" width="24px" fill="#FFFFFF"><path d="M0 0h24v24H0V0z" fill="none"/><path d="M16 11c1.66 0 2.99-1.34 2.99-3S17.66 5 16 5c-1.66 0-3 1.34-3 3s1.34 3 3 3zm-8 0c1.66 0 2.99-1.34 2.99-3S9.66 5 8 5C6.34 5 5 6.34 5 8s1.34 3 3 3zm0 2c-2.33 0-7 1.17-7 3.5V19h14v-2.5c0-2.33-4.67-3.5-7-3.5zm8 0c-.29 0-.62.02-.97.05 1.16.84 1.97 1.97 1.97 3.45V19h6v-2.5c0-2.33-4.67-3.5-7-3.5z"/></svg>;
const AdvisorIcon = () => <svg xmlns="http://www.w3.org/2000/svg" enable-background="new 0 0 24 24" height="24px" viewBox="0 0 24 24" width="24px" fill="#000000"><g><rect fill="none" height="24" width="24"/></g><g><path d="M20,2H4C2.9,2,2,2.9,2,4v18l4-4h14c1.1,0,2-0.9,2-2V4C22,2.9,21.1,2,20,2z M13.57,11.57L12,15l-1.57-3.43L7,10l3.43-1.57 L12,5l1.57,3.43L17,10L13.57,11.57z"/></g></svg>;
const UserIcon = () => <svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 0 24 24" width="24px"><path d="M0 0h24v24H0V0z" fill="none"/><path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/></svg>;
const CartIcon = () => <svg xmlns="http://www.w3.org/2000/svg" height="24" viewBox="0 0 24 24" width="24"><path d="M0 0h24v24H0z" fill="none"/><path d="M7 18c-1.1 0-1.99.9-1.99 2S5.9 22 7 22s2-.9 2-2-.9-2-2-2zM1 2v2h2l3.6 7.59-1.35 2.45c-.16.28-.25.61-.25.96 0 1.1.9 2 2 2h12v-2H7.42c-.14 0-.25-.11-.25-.25l.03-.12.9-1.63h7.45c.75 0 1.41-.41 1.75-1.03l3.58-6.49c.08-.14.12-.31.12-.48 0-.55-.45-1-1-1H5.21l-.94-2H1zm16 16c-1.1 0-1.99.9-1.99 2s.89 2 1.99 2 2-.9 2-2-.9-2-2-2z"/></svg>;
const ClipboardIcon = () => <svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 0 24 24" width="24px"><path d="M0 0h24v24H0V0z" fill="none"/><path d="M19 3h-4.18C14.4 1.84 13.3 1 12 1s-2.4.84-2.82 2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-7 0c.55 0 1 .45 1 1s-.45 1-1 1-1-.45-1-1 .45-1 1-1zm2 14H7v-2h7v2zm3-4H7v-2h10v2zm0-4H7V7h10v2z"/></svg>;
const GlobeIcon = () => <svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 0 24 24" width="24px"><path d="M0 0h24v24H0V0z" fill="none"/><path d="M12 2C6.48 2 2 6.48 2 12s4.47 10 10 10 10-4.47 10-10S17.52 2 12 2zm0 18c-4.41 0-8-3.59-8-8s3.59-8 8-8 8 3.59 8 8-3.59 8-8 8z"/><path d="M10.75 16.5c.33.23.7.35 1.08.35.38 0 .75-.12 1.08-.35 1.13-.78 1.5-2.29 1.5-3.58 0-1.28-.38-2.61-1.1-3.41-.35-.38-.8-.6-1.29-.6s-.94.22-1.29.6c-.72.8-1.1 2.13-1.1 3.41 0 1.29.37 2.8 1.5 3.58z" opacity=".3"/><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 18c-4.41 0-8-3.59-8-8s3.59-8 8-8 8 3.59 8 8-3.59 8-8 8zm-1.17-11.5c.36-.38.81-.6 1.3-.6s.93.22 1.29.6c.72.8 1.1 2.13 1.1 3.41s-.37 2.8-1.5 3.58c-.33.23-.7.35-1.08.35s-.75-.12-1.08-.35c-1.13-.78-1.5-2.29-1.5-3.58s.38-2.61 1.1-3.41z"/></svg>;
const ChevronDownIcon = () => <svg xmlns="http://www.w3.org/2000/svg" height="20px" viewBox="0 0 24 24" width="20px"><path d="M0 0h24v24H0V0z" fill="none"/><path d="M7.41 8.59L12 13.17l4.59-4.58L18 10l-6 6-6-6 1.41-1.41z"/></svg>;


const badgeTypes: Record<string, Badge> = {
    'eco-farmer': { id: 'eco-farmer', nameKey: 'badges.eco-farmer', icon: <LeafIcon />, color: '#4CAF50' },
    'trusted-buyer': { id: 'trusted-buyer', nameKey: 'badges.trusted-buyer', icon: <ShieldIcon />, color: '#2196F3' },
    'timely-delivery': { id: 'timely-delivery', nameKey: 'badges.timely-delivery', icon: <TruckIcon />, color: '#FF9800' },
    'first-purchase': { id: 'first-purchase', nameKey: 'badges.first-purchase', icon: <StarIcon filled />, color: '#FFC107' }
};

// --- App State & Components ---
type UserType = 'farmer' | 'buyer';
type View = 'login' | 'farmer' | 'buyer' | 'community';

const App: FC = () => {
    const [userType, setUserType] = useState<UserType | null>(null);
    const [view, setView] = useState<View>('login');
    const [listings, setListings] = useState<Listing[]>(initialListings);
    const [orders, setOrders] = useState<Order[]>([]);
    const [language, setLanguage] = useState<LanguageCode>('en');

    // Simulate logged in users
    const currentFarmer = mockFarmers[0];
    const currentBuyer = { name: "Rohan Verma" };

    const t = (key: string, options?: Record<string, string | number>): string => {
        const langDict = translations[language];
        let text: any = key.split('.').reduce((obj, k) => obj?.[k], langDict);
        
        if (typeof text !== 'string') {
            console.warn(`Translation key not found: ${key}`);
            return key;
        }

        if (options) {
            Object.entries(options).forEach(([optKey, value]) => {
                text = text.replace(new RegExp(`{{${optKey}}}`, 'g'), String(value));
            });
        }
        return text;
    };

    const handleLogin = (type: UserType) => {
        setUserType(type);
        setView(type);
    };

    const handleLogout = () => {
        setUserType(null);
        setView('login');
    };
    
    const navigateTo = (newView: View) => {
        setView(newView);
    };
    
    const handlePlaceOrder = (cartItems: CartItem[]) => {
        const ordersBySeller: Record<string, CartItem[]> = cartItems.reduce((acc, item) => {
            const sellerId = item.listing.seller.id;
            if (!acc[sellerId]) acc[sellerId] = [];
            acc[sellerId].push(item);
            return acc;
        }, {} as Record<string, CartItem[]>);

        const newOrders: Order[] = Object.entries(ordersBySeller).map(([sellerId, items]) => ({
            id: Date.now() + Math.random(),
            buyerName: currentBuyer.name,
            items,
            total: items.reduce((sum, item) => sum + item.listing.price * item.quantity, 0),
            sellerId: sellerId,
            status: 'Pending',
        }));

        setOrders(prevOrders => [...prevOrders, ...newOrders]);
        alert(t('alerts.orderPlaced', { count: newOrders.length }));
    };

    const handleUpdateOrderStatus = (orderId: number, newStatus: Order['status']) => {
        setOrders(orders.map(o => o.id === orderId ? { ...o, status: newStatus } : o));
    };

    const handleAddListing = (listingData: { produceName: string; price: number; unit: string; }) => {
        const { produceName, price, unit } = listingData;
        const produceTemplate = initialProduceData.find(p => p.name === produceName);

        if (!produceTemplate) {
            console.error("Could not find produce template for", produceName);
            return;
        }

        const newListing: Listing = {
            id: `${produceName.toLowerCase().replace(' ', '-')}-${currentFarmer.id}-${Date.now()}`,
            produceName,
            price,
            unit,
            img: produceTemplate.img,
            category: produceTemplate.category,
            seller: currentFarmer,
        };

        setListings(prev => [newListing, ...prev]);
    };
    
    const renderContent = () => {
        switch(view) {
            case 'login':
                return <LoginScreen onLogin={handleLogin} />;
            case 'farmer':
                return <FarmerDashboard 
                            onLogout={handleLogout} 
                            onNavigate={navigateTo} 
                            orders={orders.filter(o => o.sellerId === currentFarmer.id)}
                            currentFarmer={currentFarmer}
                            onUpdateStatus={handleUpdateOrderStatus}
                            listings={listings}
                            onAddListing={handleAddListing}
                        />;
            case 'buyer':
                return <BuyerDashboard 
                            onLogout={handleLogout} 
                            onNavigate={navigateTo}
                            listings={listings}
                            onPlaceOrder={handlePlaceOrder}
                            orders={orders.filter(o => o.buyerName === currentBuyer.name)}
                        />;
            case 'community':
                return <CommunityHub onBack={() => setView(userType || 'login')} />;
            default:
                return <LoginScreen onLogin={handleLogin} />;
        }
    }

    return (
        <LanguageContext.Provider value={{ language, setLanguage, t }}>
            <div className="app-container">
                {renderContent()}
            </div>
        </LanguageContext.Provider>
    );
};

// --- Language Selector ---
const LanguageSelector: FC = () => {
    const { language, setLanguage } = useLanguage();
    const [isOpen, setIsOpen] = useState(false);
    const wrapperRef = useRef<HTMLDivElement>(null);

    useEffect(() => {
        function handleClickOutside(event: MouseEvent) {
            if (wrapperRef.current && !wrapperRef.current.contains(event.target as Node)) {
                setIsOpen(false);
            }
        }
        document.addEventListener("mousedown", handleClickOutside);
        return () => {
            document.removeEventListener("mousedown", handleClickOutside);
        };
    }, [wrapperRef]);
    
    return (
        <div ref={wrapperRef} className="language-selector-container">
            <button onClick={() => setIsOpen(!isOpen)} className="language-selector-button" aria-haspopup="true" aria-expanded={isOpen}>
                <GlobeIcon />
                <span>{translations[language].languageName}</span>
                <ChevronDownIcon />
            </button>
            {isOpen && (
                <div className="language-dropdown" role="menu">
                    {(Object.keys(translations) as LanguageCode[]).map(langCode => (
                        <button 
                            key={langCode} 
                            onClick={() => { setLanguage(langCode); setIsOpen(false); }}
                            role="menuitem"
                        >
                            {translations[langCode].languageName}
                        </button>
                    ))}
                </div>
            )}
        </div>
    );
};

// --- Login Screen ---
const LoginScreen: FC<{ onLogin: (type: UserType) => void }> = ({ onLogin }) => {
    const { t } = useLanguage();

    return (
        <div className="login-screen">
            <LanguageSelector />
            <header><h1>{t('app.title')}</h1><p>{t('app.subtitle')}</p></header>
            <div className="role-selection">
                <button className="farmer" onClick={() => onLogin('farmer')}>{t('login.farmerButton')}</button>
                <button className="buyer" onClick={() => onLogin('buyer')}>{t('login.buyerButton')}</button>
            </div>
            <div className="auth-methods">
                 <button className="google-signin-btn">
                    <GoogleIcon /><span>{t('login.googleSignIn')}</span>
                </button>
                <p>{t('login.orSignInWith')}</p>
                <div className="auth-icons"><FingerprintIcon /></div>
            </div>
        </div>
    );
};

// --- Farmer Dashboard ---
type FarmerSubView = 'dashboard' | 'profile' | 'orders';
type FarmerSortOrder = 'newest-first' | 'oldest-first' | 'status';

interface FarmerDashboardProps {
    onLogout: () => void;
    onNavigate: (view: View) => void;
    orders: Order[];
    currentFarmer: Farmer;
    onUpdateStatus: (orderId: number, status: Order['status']) => void;
    listings: Listing[];
    onAddListing: (data: { produceName: string; price: number; unit: string; }) => void;
}

const FarmerDashboard: FC<FarmerDashboardProps> = ({ 
    onLogout, onNavigate, orders, currentFarmer, onUpdateStatus, listings, onAddListing 
}) => {
    const { t } = useLanguage();
    const [cropName, setCropName] = useState(uniqueProduceNames[0] || 'Tomatoes');
    const [priceInput, setPriceInput] = useState('');
    const [unitInput, setUnitInput] = useState('kg');
    const [tip, setTip] = useState<string | null>(null);
    const [loading, setLoading] = useState<string | null>(null);
    const [error, setError] = useState<string | null>(null);
    const [badges, setBadges] = useState<Badge[]>([badgeTypes['eco-farmer']]);
    const [farmerView, setFarmerView] = useState<FarmerSubView>('dashboard');
    const [sortOrder, setSortOrder] = useState<FarmerSortOrder>('newest-first');

    const sortedOrders = useMemo(() => {
        const sorted = [...orders];
        switch (sortOrder) {
            case 'oldest-first':
                sorted.sort((a, b) => a.id - b.id);
                break;
            case 'status':
                const statusOrder: Record<Order['status'], number> = { 'Pending': 1, 'Shipped': 2, 'Delivered': 3 };
                sorted.sort((a, b) => statusOrder[a.status] - statusOrder[b.status]);
                break;
            case 'newest-first':
            default:
                sorted.sort((a, b) => b.id - a.id);
                break;
        }
        return sorted;
    }, [orders, sortOrder]);

    const myListings = useMemo(() => {
        return listings.filter(l => l.seller.id === currentFarmer.id);
    }, [listings, currentFarmer.id]);

    const handleGetAIPrice = async () => {
        setLoading('price');
        setError(null);
        try {
            const response = await ai.models.generateContent({
                model: 'gemini-2.5-flash',
                contents: `Suggest a fair market price per ${unitInput} for high-quality ${cropName} in a major Indian metropolitan area. Provide only a single number representing the price in INR, for example: 45.50.`,
            });
            const suggestedPrice = response.text.trim();
            if (!isNaN(parseFloat(suggestedPrice))) {
                 setPriceInput(parseFloat(suggestedPrice).toFixed(2));
            } else {
                throw new Error("Received an invalid price format from the AI.");
            }
        } catch (err) {
            console.error(err);
            setError(t('farmer.error.aiPrice'));
        } finally {
            setLoading(null);
        }
    };
    
    const handleListProduce = (e: FormEvent) => {
        e.preventDefault();
        if (!cropName || !priceInput || !unitInput || isNaN(parseFloat(priceInput))) {
            setError(t('farmer.error.formValidation'));
            setTimeout(() => setError(null), 3000);
            return;
        }
        setError(null);
        onAddListing({
            produceName: cropName,
            price: parseFloat(priceInput),
            unit: unitInput,
        });
        setPriceInput(''); // Clear price after listing
    };

    const getCropTip = async () => {
        setLoading('tip');
        setError(null);
        setTip(null);
        try {
            const response = await ai.models.generateContent({
                model: 'gemini-2.5-flash',
                contents: `Provide a short, actionable crop health tip for a small-scale farmer growing ${cropName || 'vegetables'}. Focus on a practical, organic solution.`,
            });
            setTip(response.text);
        } catch (err) {
            console.error(err);
            setError(t('farmer.error.cropTip'));
        } finally {
            setLoading(null);
        }
    }

    const handleCompleteDelivery = () => {
        if (!badges.some(b => b.id === 'timely-delivery')) {
            setBadges([...badges, badgeTypes['timely-delivery']]);
            alert(t('alerts.newBadge', { badgeName: t('badges.timely-delivery') }));
        } else {
            alert(t('alerts.consistentDelivery'));
        }
    };

    const renderHeaderTitle = () => {
        switch (farmerView) {
            case 'profile': return t('farmer.profileTitle');
            case 'orders': return t('farmer.ordersTitle');
            default: return t('farmer.dashboardTitle');
        }
    };

    const renderOrderStatusButton = (order: Order) => {
        if (order.status === 'Pending') {
            return <button onClick={() => onUpdateStatus(order.id, 'Shipped')} className="btn btn-action">{t('farmer.orders.markShipped')}</button>;
        }
        if (order.status === 'Shipped') {
            return <button onClick={() => onUpdateStatus(order.id, 'Delivered')} className="btn btn-action">{t('farmer.orders.markDelivered')}</button>;
        }
        return <button className="btn" disabled>{t('farmer.orders.delivered')}</button>;
    };

    const renderContent = () => {
        switch (farmerView) {
            case 'orders':
                return (
                    <div className="view-container">
                        <div className="order-controls">
                            <label htmlFor="sort-orders">{t('farmer.orders.sortBy')}</label>
                            <select id="sort-orders" value={sortOrder} onChange={e => setSortOrder(e.target.value as FarmerSortOrder)}>
                                <option value="newest-first">{t('farmer.orders.sort.newest')}</option>
                                <option value="oldest-first">{t('farmer.orders.sort.oldest')}</option>
                                <option value="status">{t('farmer.orders.sort.status')}</option>
                            </select>
                        </div>
                        {sortedOrders.length > 0 ? (
                            sortedOrders.map(order => (
                                <div key={order.id} className="card order-card">
                                    <div className="order-header">
                                        <h4>{t('farmer.orders.orderFrom', {name: order.buyerName})}</h4>
                                        <span className={`status-tag status-${order.status.toLowerCase()}`}>{t(`status.${order.status.toLowerCase()}`)}</span>
                                    </div>
                                    <p className="order-id">ID: {order.id}</p>
                                    {order.items.map(item => (
                                        <div key={item.listing.id} className="order-item">
                                            <span>{item.listing.produceName} ({item.quantity} {item.listing.unit})</span>
                                            <span>₹{(item.listing.price * item.quantity).toFixed(2)}</span>
                                        </div>
                                    ))}
                                    <div className="order-total">
                                        <strong>{t('farmer.orders.total')}</strong>
                                        <strong>₹{order.total.toFixed(2)}</strong>
                                    </div>
                                    <div className="order-actions">
                                        {renderOrderStatusButton(order)}
                                    </div>
                                </div>
                            ))
                        ) : (
                            <p className="empty-message">{t('farmer.orders.noOrders')}</p>
                        )}
                        <button onClick={() => setFarmerView('dashboard')} className="btn btn-secondary">{t('farmer.profile.backToDashboard')}</button>
                    </div>
                );
            case 'profile':
                return (
                    <div className="view-container">
                        <div className="card kisan-card">
                            <img src="https://images.unsplash.com/photo-1554151228-14d9def656e4?q=80&w=250" alt="Farmer profile" />
                            <div className="kisan-card-info">
                                <h4>{currentFarmer.name}</h4>
                                <p>{t('farmer.profile.kisanCard')}</p>
                                <div className="badges-container">
                                    {badges.map(badge => (
                                    <div key={badge.id} className="badge" style={{ backgroundColor: badge.color }} data-tooltip={t(badge.nameKey)}>
                                        {badge.icon}
                                    </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                        <div className="card">
                            <h3>{t('farmer.profile.rewardsTitle')}</h3>
                            <p>{t('farmer.profile.rewardsDesc')}</p>
                            <button onClick={handleCompleteDelivery} className="btn btn-action">
                                {t('farmer.profile.simulateDelivery')}
                            </button>
                        </div>
                        <button onClick={onLogout} className="btn btn-secondary logout-full-btn">
                           <LogoutIcon /> {t('farmer.profile.logout')}
                        </button>
                        <button onClick={() => setFarmerView('dashboard')} className="btn btn-secondary">{t('farmer.profile.backToDashboard')}</button>
                    </div>
                );
            default: // dashboard
                return (
                     <div className="view-container">
                        <div className="card">
                            <button onClick={() => onNavigate('community')} className="btn btn-nav">
                                <CommunityIcon />
                                <span>{t('farmer.communityButton')}</span>
                            </button>
                        </div>
                        <div className="card">
                            <h3>{t('farmer.listProduceTitle')}</h3>
                            <form onSubmit={handleListProduce}>
                                <div className="form-group">
                                    <label htmlFor="cropName">{t('farmer.cropNameLabel')}</label>
                                    <select id="cropName" value={cropName} onChange={e => setCropName(e.target.value)}>
                                        {uniqueProduceNames.map(name => (
                                            <option key={name} value={name}>{name}</option>
                                        ))}
                                    </select>
                                </div>
                                <div className="form-group">
                                    <label htmlFor="unit">{t('farmer.unitLabel')}</label>
                                    <input type="text" id="unit" value={unitInput} onChange={e => setUnitInput(e.target.value)} required />
                                </div>
                                <div className="form-group">
                                    <label htmlFor="price">{t('farmer.priceLabel')}</label>
                                    <div className="price-input-group">
                                        <input type="number" id="price" placeholder={t('placeholders.price')} value={priceInput} onChange={e => setPriceInput(e.target.value)} required step="0.5" />
                                        <button type="button" className="btn-ai-suggestion" onClick={handleGetAIPrice} disabled={loading === 'price'} aria-label="Get AI price suggestion">
                                            {loading === 'price' ? '...' : <AdvisorIcon />}
                                        </button>
                                    </div>
                                </div>
                                <button type="submit" className="btn" disabled={loading === 'price'}>{t('farmer.listCropButton')}</button>
                            </form>
                        </div>
                        {myListings.length > 0 && (
                            <div className="card">
                                <h3>{t('farmer.myCropsTitle')}</h3>
                                <div className="my-listings-grid">
                                    {myListings.map(listing => (
                                        <div key={listing.id} className="my-listing-card">
                                            <img src={listing.img} alt={listing.produceName} />
                                            <div className="my-listing-info">
                                                <h5>{listing.produceName}</h5>
                                                <p>₹{listing.price} / {listing.unit}</p>
                                            </div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        )}
                        <div className="card">
                            <h3>{t('farmer.cropHealthTitle')}</h3>
                            <button onClick={getCropTip} className="btn" disabled={loading === 'tip'}>
                                {loading === 'tip' ? t('farmer.gettingTip') : t('farmer.getTipButton', { cropName: cropName || 'Crops'})}
                            </button>
                            {tip && <div className="tip-content">{tip}</div>}
                        </div>
                    </div>
                );
        }
    };

    return (
        <>
            <div className="dashboard-header">
                <h2>{renderHeaderTitle()}</h2>
                <div className="header-actions">
                     <button onClick={() => setFarmerView('orders')} className="header-icon-btn" aria-label="View Orders">
                        <ClipboardIcon />
                        {orders.filter(o => o.status === 'Pending').length > 0 && 
                            <span className="cart-badge">{orders.filter(o => o.status === 'Pending').length}</span>}
                    </button>
                    <button onClick={() => setFarmerView('profile')} className="header-icon-btn" aria-label="View Profile">
                        <UserIcon />
                    </button>
                </div>
            </div>
            {renderContent()}
            {error && <p className="error-message">{error}</p>}
        </>
    );
};

// --- Add to Cart Modal ---
const AddToCartModal: FC<{
    listing: Listing;
    onClose: () => void;
    onAddToCart: (quantity: number) => void;
}> = ({ listing, onClose, onAddToCart }) => {
    const { t } = useLanguage();
    const [quantity, setQuantity] = useState(1);

    const handleFormSubmit = (e: FormEvent) => {
        e.preventDefault();
        if (quantity > 0) {
            onAddToCart(quantity);
        }
    };

    return (
        <div className="modal-overlay" onClick={onClose}>
            <div className="modal-container" onClick={(e) => e.stopPropagation()}>
                <div className="modal-header">
                    <h3>{t('modal.addToCartTitle', {produce: listing.produceName})}</h3>
                    <button onClick={onClose} className="btn-close-modal">&times;</button>
                </div>
                <div className="modal-body">
                    <img src={listing.img} alt={listing.produceName} className="modal-item-img"/>
                    <h4>{t('modal.from', {name: listing.seller.name})}</h4>
                    <p>{t('modal.price', {price: listing.price, unit: listing.unit})}</p>
                    <form onSubmit={handleFormSubmit}>
                        <div className="form-group">
                            <label htmlFor="quantity">{t('modal.quantity', {unit: listing.unit})}</label>
                            <input
                                type="number"
                                id="quantity"
                                value={quantity}
                                onChange={(e) => setQuantity(Number(e.target.value))}
                                min="1"
                                step="0.5"
                                autoFocus
                                className="quantity-input"
                            />
                        </div>
                        <button type="submit" className="btn btn-buy">{t('modal.addButton')}</button>
                    </form>
                </div>
            </div>
        </div>
    );
};

const RatingStars: FC<{ rating: number }> = ({ rating }) => (
    <div className="rating-stars">
        {[...Array(5)].map((_, i) => <StarIcon key={i} filled={i < rating} />)}
    </div>
);

// --- Buyer Dashboard ---
type BuyerSubView = 'marketplace' | 'cart' | 'profile' | 'sellers' | 'orders';
type BuyerOrderFilter = 'all' | 'Pending' | 'Shipped' | 'Delivered';

const BuyerDashboard: FC<{ 
    onLogout: () => void; 
    onNavigate: (view: View) => void; 
    listings: Listing[];
    orders: Order[];
    onPlaceOrder: (cart: CartItem[]) => void;
}> = ({ onLogout, onNavigate, listings, orders, onPlaceOrder }) => {
    const { t } = useLanguage();
    const [bundle, setBundle] = useState<{bundleName: string, items: string[]} | null>(null);
    const [loading, setLoading] = useState(false);
    const [error, setError] = useState<string | null>(null);
    const [searchTerm, setSearchTerm] = useState('');
    const [priceFilter, setPriceFilter] = useState('all');
    const [categoryFilter, setCategoryFilter] = useState('all');
    const [badges, setBadges] = useState<Badge[]>([badgeTypes['trusted-buyer']]);
    const [cart, setCart] = useState<CartItem[]>([]);
    const [listingForCart, setListingForCart] = useState<Listing | null>(null);
    const [buyerView, setBuyerView] = useState<BuyerSubView>('marketplace');
    const [viewingProduce, setViewingProduce] = useState<string | null>(null);
    const [orderFilter, setOrderFilter] = useState<BuyerOrderFilter>('all');
    
    const filteredOrders = useMemo(() => {
        const sorted = [...orders].sort((a, b) => b.id - a.id); // Always show newest first
        if (orderFilter === 'all') {
            return sorted;
        }
        return sorted.filter(o => o.status === orderFilter);
    }, [orders, orderFilter]);

    const uniqueProduce = useMemo(() => {
        let result = listings.reduce((acc, listing) => {
            if (!acc.some(l => l.produceName === listing.produceName)) {
                acc.push(listing);
            }
            return acc;
        }, [] as Listing[]);
        
        // Apply filters
        if (searchTerm) {
            result = result.filter(item =>
                item.produceName.toLowerCase().includes(searchTerm.toLowerCase())
            );
        }
        if (priceFilter === 'under50') {
            result = result.filter(item => item.price < 50);
        } else if (priceFilter === '50plus') {
            result = result.filter(item => item.price >= 50);
        }
        if (categoryFilter !== 'all') {
            result = result.filter(item => item.category === categoryFilter);
        }
        return result;
    }, [searchTerm, priceFilter, categoryFilter, listings]);
    
    const cartTotal = useMemo(() => {
        return cart.reduce((total, cartItem) => total + cartItem.listing.price * cartItem.quantity, 0);
    }, [cart]);

    const getRecommendedBundle = async () => {
        setLoading(true);
        setError(null);
        setBundle(null);
        try {
            const availableProduce = listings.map(p => p.produceName).join(', ');
            const response = await ai.models.generateContent({
                model: "gemini-2.5-flash",
                contents: `From the available produce: ${availableProduce}, create a "Weekly Sabzi Mandi Box" for a small Indian family. Respond in JSON.`,
                config: {
                    responseMimeType: "application/json",
                    responseSchema: {
                        type: Type.OBJECT,
                        properties: {
                            bundleName: { type: Type.STRING },
                            items: { type: Type.ARRAY, items: { type: Type.STRING } }
                        },
                        required: ["bundleName", "items"]
                    }
                }
            });
            setBundle(JSON.parse(response.text.trim()));
        } catch (err) {
            console.error(err);
            setError(t('buyer.error.bundle'));
        } finally {
            setLoading(false);
        }
    };

    const handleAddToCart = (quantity: number) => {
        if (!listingForCart) return;

        setCart(prevCart => {
            const existingItem = prevCart.find(ci => ci.listing.id === listingForCart.id);
            if (existingItem) {
                return prevCart.map(ci => 
                    ci.listing.id === listingForCart.id
                        ? { ...ci, quantity: ci.quantity + quantity } 
                        : ci
                );
            } else {
                return [...prevCart, { listing: listingForCart, quantity }];
            }
        });
        
        if (!badges.some(b => b.id === 'first-purchase') && cart.length === 0) {
            setBadges([...badges, badgeTypes['first-purchase']]);
             setTimeout(() => alert(t('alerts.firstPurchase')), 100);
        }

        setListingForCart(null); // Close modal
    };
    
    const handleRemoveFromCart = (listingId: string) => {
        setCart(prevCart => prevCart.filter(ci => ci.listing.id !== listingId));
    };

    const handleCheckout = () => {
        onPlaceOrder(cart);
        setCart([]);
        setBuyerView('marketplace');
    };

    const renderHeaderTitle = () => {
        switch (buyerView) {
            case 'cart': return t('buyer.cartTitle');
            case 'profile': return t('buyer.profileTitle');
            case 'sellers': return t('buyer.sellersTitle', {produce: viewingProduce || ''});
            case 'orders': return t('buyer.ordersTitle');
            default: return t('buyer.marketplaceTitle');
        }
    }

    const renderContent = () => {
        if (buyerView === 'sellers' && viewingProduce) {
            const sellersForProduce = listings.filter(l => l.produceName === viewingProduce).sort((a, b) => a.price - b.price);
            return (
                <div className="view-container">
                    <div className="card">
                        {sellersForProduce.map(listing => (
                            <div key={listing.id} className="seller-listing-card">
                                <div className="seller-info">
                                    <div className="seller-name-rating">
                                        <strong>{listing.seller.name}</strong>
                                        <RatingStars rating={listing.seller.rating} />
                                    </div>
                                    <span className="seller-location">{listing.seller.location}</span>
                                </div>
                                <div className="seller-price-action">
                                    <span className="seller-price">₹{listing.price} / {listing.unit}</span>
                                    <button className="btn-buy" onClick={() => setListingForCart(listing)}>Add</button>
                                </div>
                            </div>
                        ))}
                    </div>
                    <button onClick={() => setBuyerView('marketplace')} className="btn btn-secondary">{t('buyer.profile.backToMarket')}</button>
                </div>
            )
        }
        
        switch (buyerView) {
            case 'orders':
                return (
                    <div className="view-container">
                         <div className="order-controls filter-buttons">
                            {(['all', 'Pending', 'Shipped', 'Delivered'] as const).map(status => (
                                <button 
                                    key={status} 
                                    onClick={() => setOrderFilter(status)}
                                    className={orderFilter === status ? 'active' : ''}
                                >
                                    {t(`buyer.orders.filter.${status.toLowerCase() as any}`)}
                                </button>
                            ))}
                         </div>
                         {filteredOrders.length > 0 ? (
                            filteredOrders.map(order => (
                                <div key={order.id} className="card order-card">
                                    <div className="order-header">
                                        <h4>{t('buyer.orders.orderTo', {name: listings.find(l => l.seller.id === order.sellerId)?.seller.name || 'a farmer'})}</h4>
                                        <span className={`status-tag status-${order.status.toLowerCase()}`}>{t(`status.${order.status.toLowerCase()}`)}</span>
                                    </div>
                                    <p className="order-id">ID: {order.id}</p>
                                    {order.items.map(item => (
                                        <div key={item.listing.id} className="order-item">
                                            <span>{item.listing.produceName} ({item.quantity} {item.listing.unit})</span>
                                            <span>₹{(item.listing.price * item.quantity).toFixed(2)}</span>
                                        </div>
                                    ))}
                                    <div className="order-total">
                                        <strong>{t('buyer.cart.total')}</strong>
                                        <strong>₹{order.total.toFixed(2)}</strong>
                                    </div>
                                </div>
                            ))
                        ) : (
                            <p className="empty-message">{t('buyer.orders.noOrders')}</p>
                        )}
                        <button onClick={() => setBuyerView('marketplace')} className="btn btn-secondary">{t('buyer.profile.backToMarket')}</button>
                    </div>
                );
            case 'cart':
                return (
                    <div className="view-container">
                        {cart.length > 0 ? (
                            <div className="card cart-card">
                                <div className="cart-items-list">
                                    {cart.map(cartItem => (
                                        <div key={cartItem.listing.id} className="cart-item">
                                            <div className="cart-item-info">
                                                <strong>{cartItem.listing.produceName}</strong>
                                                <span>{cartItem.quantity} {cartItem.listing.unit} &times; ₹{cartItem.listing.price}</span>
                                                <small>{t('buyer.cart.from', {name: cartItem.listing.seller.name})}</small>
                                            </div>
                                            <div className="cart-item-controls">
                                                <span>₹{(cartItem.listing.price * cartItem.quantity).toFixed(2)}</span>
                                                <button onClick={() => handleRemoveFromCart(cartItem.listing.id)} className="btn-remove-item">&times;</button>
                                            </div>
                                        </div>
                                    ))}
                                </div>
                                <div className="cart-total">
                                    <strong>{t('buyer.cart.total')}</strong>
                                    <strong>₹{cartTotal.toFixed(2)}</strong>
                                </div>
                                <button className="btn btn-buy btn-checkout" onClick={handleCheckout}>{t('buyer.cart.checkout')}</button>
                            </div>
                        ) : (
                           <p className="empty-message">{t('buyer.cart.empty')}</p>
                        )}
                        <button onClick={() => setBuyerView('marketplace')} className="btn btn-secondary">{t('buyer.cart.backToMarket')}</button>
                    </div>
                );
            case 'profile':
                return (
                     <div className="view-container">
                         <div className="card buyer-profile-card">
                            <img src="https://images.unsplash.com/photo-1570295999919-56ceb5ecca61?q=80&w=250" alt="Buyer profile" />
                            <div className="buyer-profile-info">
                                <h4>Rohan Verma</h4>
                                <p>{t('buyer.profile.valuedCustomer')}</p>
                                <div className="badges-container">
                                    {badges.map(badge => (
                                    <div key={badge.id} className="badge" style={{ backgroundColor: badge.color }} data-tooltip={t(badge.nameKey)}>
                                        {badge.icon}
                                    </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                        <div className="card"><button onClick={() => onNavigate('community')} className="btn btn-nav"><CommunityIcon /><span>{t('buyer.profile.communityHub')}</span></button></div>
                        <div className="card">
                            <h3>{t('buyer.profile.aiBundleTitle')}</h3>
                            <button className="btn" onClick={getRecommendedBundle} disabled={loading}>{loading ? t('buyer.profile.aiBundleGenerating') : t('buyer.profile.aiBundleButton')}</button>
                            {bundle && (
                                <div className="bundle-content">
                                    <h4>{bundle.bundleName}</h4>
                                    <ul>{bundle.items.map((item, index) => <li key={index}>{item}</li>)}</ul>
                                </div>
                            )}
                        </div>
                        <button onClick={onLogout} className="btn btn-secondary logout-full-btn"><LogoutIcon/> {t('buyer.profile.logout')}</button>
                        <button onClick={() => setBuyerView('marketplace')} className="btn btn-secondary">{t('buyer.profile.backToMarket')}</button>
                    </div>
                );
            default: // marketplace
                return (
                    <div className="view-container">
                        <input type="text" placeholder={t('buyer.searchPlaceholder')} className="search-bar" value={searchTerm} onChange={e => setSearchTerm(e.target.value)} />
                        <div className="filter-controls">
                            <div className="form-group"><label htmlFor="priceFilter">{t('buyer.filters.price')}</label>
                                <select id="priceFilter" value={priceFilter} onChange={e => setPriceFilter(e.target.value)}>
                                    <option value="all">{t('buyer.filters.all')}</option><option value="under50">{t('buyer.filters.under50')}</option><option value="50plus">{t('buyer.filters.over50')}</option>
                                </select>
                            </div>
                            <div className="form-group"><label htmlFor="categoryFilter">{t('buyer.filters.category')}</label>
                                <select id="categoryFilter" value={categoryFilter} onChange={e => setCategoryFilter(e.target.value)}>
                                    <option value="all">{t('buyer.filters.all')}</option><option value="Vegetable">{t('buyer.filters.vegetables')}</option><option value="Fruit">{t('buyer.filters.fruits')}</option>
                                </select>
                            </div>
                        </div>
                        <div className="card">
                            <h3>{t('buyer.freshFromFarms')}</h3>
                            <div className="marketplace-grid">
                                {uniqueProduce.length > 0 ? (
                                    uniqueProduce.map(item => (
                                        <div key={item.produceName} className="produce-card" onClick={() => { setBuyerView('sellers'); setViewingProduce(item.produceName); }}>
                                            <img src={item.img} alt={item.produceName} />
                                            <div className="produce-info">
                                                <h5>{item.produceName}</h5>
                                                <p>{t('buyer.fromPrice', { price: Math.min(...listings.filter(l => l.produceName === item.produceName).map(l => l.price)) })}</p>
                                                <button className="btn-buy">{t('buyer.viewSellers')}</button>
                                            </div>
                                        </div>
                                    ))
                                ) : (
                                    <p className="no-results">{t('buyer.noResults')}</p>
                                )}
                            </div>
                        </div>
                    </div>
                );
        }
    }

    return (
        <>
            {listingForCart && (
                <AddToCartModal
                    listing={listingForCart}
                    onClose={() => setListingForCart(null)}
                    onAddToCart={handleAddToCart}
                />
            )}
            <div className="dashboard-header">
                <h2>{renderHeaderTitle()}</h2>
                <div className="header-actions">
                    <button onClick={() => setBuyerView('orders')} className="header-icon-btn" aria-label="View Orders">
                        <ClipboardIcon />
                    </button>
                    <button onClick={() => setBuyerView('cart')} className="header-icon-btn" aria-label="View Cart">
                        <CartIcon />
                        {cart.length > 0 && <span className="cart-badge">{cart.length}</span>}
                    </button>
                    <button onClick={() => setBuyerView('profile')} className="header-icon-btn" aria-label="View Profile">
                        <UserIcon />
                    </button>
                </div>
            </div>
            {renderContent()}
            {error && <p className="error-message">{error}</p>}
        </>
    );
};

// --- Community Hub ---
interface QAPair { id: number; question: string; answer: string; }

const CommunityHub: FC<{ onBack: () => void }> = ({ onBack }) => {
    const { t } = useLanguage();
    const [newQuestion, setNewQuestion] = useState('');
    const [qaList, setQAList] = useState<QAPair[]>([]);
    const [loading, setLoading] = useState(false);
    const [error, setError] = useState<string | null>(null);

    const handleAskQuestion = async (e: FormEvent) => {
        e.preventDefault();
        if (!newQuestion.trim()) return;
        setLoading(true);
        setError(null);
        try {
            const response = await ai.models.generateContent({
                model: 'gemini-2.5-flash',
                contents: `You are an expert AI Crop Advisor for FarmConnect+. A user has asked: "${newQuestion}". Provide a helpful, concise answer for a farmer in India.`,
            });
            const newEntry: QAPair = { id: Date.now(), question: newQuestion, answer: response.text };
            setQAList([newEntry, ...qaList]);
            setNewQuestion('');
        } catch (err) {
            setError(t('community.error'));
        } finally { setLoading(false); }
    };

    return (
        <div className="community-hub">
            <div className="dashboard-header">
                <h2>{t('community.title')}</h2>
                <button onClick={onBack} className="btn-back">{t('community.back')}</button>
            </div>
            <div className="card">
                <h3>{t('community.askTitle')}</h3>
                <form onSubmit={handleAskQuestion} className="qa-form">
                    <textarea value={newQuestion} onChange={e => setNewQuestion(e.target.value)} placeholder={t('community.askPlaceholder')} rows={4} disabled={loading} />
                    <button type="submit" className="btn" disabled={loading || !newQuestion.trim()}>{loading ? t('community.askThinking') : t('community.askButton')}</button>
                </form>
            </div>
            <div className="qa-list">
                {qaList.map(item => (
                    <div key={item.id} className="card qa-item">
                        <div className="qa-question"><UserIcon/> <p>{item.question}</p></div>
                        <div className="qa-answer"><AdvisorIcon /> <p>{item.answer}</p></div>
                    </div>
                ))}
            </div>
            {error && <p className="error-message">{error}</p>}
        </div>
    );
};


// --- Render App ---
const container = document.getElementById('root');
if (container) {
    const root = createRoot(container);
    root.render(<App />);
}
